@model WebSimpleContent

@if (Model != null)
{
    <section class="hero hero-overlap" style="background-image: url('@Model.Link');">
        <div class="bg-overlay">
            <div class="container">
                <div class="intro-wrap">
                    @*<h1 class="intro-title">Where the Journey Begins</h1>
                        <div class="intro-text"> Discover your next great adventure, <a href="#">become an explorer</a> to get started!</div>*@
                    @Html.Raw(Model.Body)
                </div>
            </div>
        </div>
    </section>

}